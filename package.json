{
  "comments": {
    "dependencies": {},
    "devDependencies": {}
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "@tailwindcss/typography": "^0.5.0-alpha.3",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.1",
    "tailwindcss": "^4.0.0"
  },
  "name": "johnkirwan",
  "scripts": {
    "preinstall": "node -e \"if(!(process.env.npm_config_user_agent||'').startsWith('npm/')){console.error('This project uses npm. Run: npm install (not pnpm/yarn) - Hugo 0.163 needs npm-style Tailwind bins.');process.exit(1)}\"",
    "dev": "hugo server -D",
    "resume:build": "node scripts/build-resume.mjs",
    "resume:pdf": "node scripts/build-resume.mjs --pdf"
  },
  "version": "0.1.0"
}
